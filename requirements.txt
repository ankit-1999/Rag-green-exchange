# GreenGrid Exchange RAG AI — Python dependencies

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Data validation
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Environment variable loading
python-dotenv>=1.0.0

# AWS SDK — Bedrock, S3, Secrets Manager
boto3>=1.34.0

# OpenSearch client with AWS SigV4 auth
opensearch-py>=2.6.0
requests-aws4auth>=1.3.0

# HTTP client (used by opensearch-py)
requests>=2.32.0
